import { SimplePage } from "@/components/SimplePage";

export default function LoginPage() {
  return <SimplePage eyebrow="Centre login" title="Centre login coming soon" text="A secure account area can be connected when listing management is added." />;
}
